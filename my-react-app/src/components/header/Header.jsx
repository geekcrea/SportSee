import Logo from './Logo';
import Navbar from './Navbar';

// function to render the header
function Header() {
  return (
    <header>
      <Logo />
      <Navbar />
    </header>
  );
}

export default Header;